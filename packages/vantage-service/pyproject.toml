[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "vantage-service"
version = "0.1.0"
description = "Vantage service: read API and web interface. Skeleton only -- no code until Milestone 4."
license = "MIT"
requires-python = ">=3.10,<3.14"
authors = [{ name = "Guillermo Gil" }]
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["src/vantage_service"]
