"""The v1 SQLite storage adapter (design SS2). `tables.py` is the schema
authority; `engine.py` wires WAL pragmas; `repository.py` implements the
`vantage.core.storage.repository` Protocols against it.
"""
